# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: tests/login.spec.js >> Login Tests (same page) >> Login Error validation
- Location: tests/login.spec.js:25:3

# Error details

```
Error: locator._expect: selector: expected string, got function
```

# Test source

```ts
  1  | const { test, expect, chromium } = require('@playwright/test');
  2  | const LoginPage = require('../pages/LoginPage');
  3  | 
  4  | let page;
  5  | let browser;
  6  | 
  7  | test.describe('Login Tests (same page)', () => {
  8  | 
  9  |   test.beforeAll(async () => {
  10 |     browser = await chromium.launch({ headless: false });
  11 |     page = await browser.newPage();
  12 | 
  13 |     await page.goto('https://www.facebook.com/');
  14 |   });
  15 | 
  16 |   test.afterAll(async () => {
  17 |     await browser.close();
  18 |   });
  19 | 
  20 |   test('Login Test', async () => {
  21 |     const loginPage = new LoginPage(page);
  22 |     await loginPage.login('9848725287', 'Pnssss');
  23 |   });
  24 | 
  25 |   test('Login Error validation', async () => {
  26 |     const loginPage = new LoginPage(page);
  27 |     await loginPage.login('9848725287', 'wrongpassword');
  28 | 
  29 |     await expect(page.locator(loginPage.loginError))
> 30 |       .toContainText('incorrect');
     |        ^ Error: locator._expect: selector: expected string, got function
  31 |   });
  32 | 
  33 | });
```